#import "../theorem/core.typ": generic-box

// define questions
#let generic-question = generic-box.with(
  kind: "question"
)

#let generic-subquestion = generic-box.with(
  kind: "subquestion"
)

#let points-box = generic-box.with(
  kind: "pts-box"
)
